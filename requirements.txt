# SoloPilot Requirement Analyser Dependencies
# Core ML and processing dependencies

# LangChain for LLM orchestration (light usage) - compatible versions
langchain>=0.3.25,<1.0.0
langchain-community==0.3.24
langchain-core>=0.3.55,<1.0.0

# AWS Bedrock integration (primary LLM)
langchain-aws==0.2.24
boto3>=1.34.120
botocore>=1.34.120


# OCR and image processing
pytesseract>=0.3.10
Pillow>=10.0.0

# Vector similarity search and context storage
# ChromaDB for persistent context storage and retrieval
chromadb>=0.4.15,<1.0.0

# Vector similarity search (use conda-forge or brew version if faiss-cpu fails)
# Option 1: Try faiss-cpu first
faiss-cpu>=1.11.0; platform_system != "Darwin"
# Option 2: For macOS, use scikit-learn as fallback for vector similarity
scikit-learn==1.5.2; platform_system == "Darwin"

# Configuration and environment
python-dotenv==1.0.1
PyYAML==6.0.2

# HTTP client for API calls
requests==2.32.3

# Testing framework
pytest==8.3.3

# Additional parsing support
python-docx==1.1.2

# Data validation
pydantic>=2.10.0,<3.0.0

# Development dependencies
black==24.8.0
flake8==7.1.1

# Optional: Jupyter for interactive development
# jupyter==1.1.1  # Uncomment if needed for development

# Serena LSP Integration for symbol-aware context management
# Installed via uvx: uvx --from git+https://github.com/oraios/serena install